import 'package:flutter/material.dart';

class Add_taskScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add task'),
      ),
      body: Center(
        child: Text('This is the Add task screen'),
      ),
    );
  }
}
