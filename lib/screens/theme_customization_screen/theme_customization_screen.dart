import 'package:flutter/material.dart';

class Theme_customizationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Theme customization'),
      ),
      body: Center(
        child: Text('This is the Theme customization screen'),
      ),
    );
  }
}
