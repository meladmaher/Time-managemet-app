import 'package:flutter/material.dart';

class Todo_listScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Todo list'),
      ),
      body: Center(
        child: Text('This is the Todo list screen'),
      ),
    );
  }
}
